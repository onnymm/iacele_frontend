import Group from "../../components/layout/Group";

const Home: () => (React.JSX.Element) = () => {

    return (
        <Group>
            Contenido
        </Group>
    );
}

export default Home;
